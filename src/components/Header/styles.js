import styled from 'styled-components';

export const Inner = styled.div`
	position: fixed;
	top: 0;
	left: 0;
	width: 100%;
	z-index: 100;

	& > div {
		position: relative;
	}
`;

// export const Header = styled.div``;
